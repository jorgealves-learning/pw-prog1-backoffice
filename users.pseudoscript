importar Billing

modelo User:
    construtor(email, password):
        User.email = email
        User.password = password
        User.listausers = ()

funcao AddNewUser(email, password):
        User.email = email